### Checklist (Don't remove in order to have structure when copy-past)

1. Update project created for "business name"
2. Update `robots.ts` and `sitemap.ts` (if needed)
3. Update `opengraph-image.jpg` (1200x630) (create using photopea - save .psd as well)
4. Update `package.json`
5. Update `logo.jpg`
6. Update `favicon.ico`
7. Update `css` and `UI` using AI according to design that client wants
8. Update `businessInfo.ts` and `consts.ts`
9. Update `contactUsAction` (with html) and `bookAppointmentFn` and `cancelAppointmentFn`
   and any mentions about `resend` or `SMS` or `twilio` - receive notification SMS or Email or both?
10. Update prompt in `generateImagesAction` to generate images in proper style
11. Do stuff that written in `Calendar/dev_readme`
12. Update `.env.local`

---

### Project info

This project created for "obsidian auto car" business
Agreement: teseimonial

### TODO

This project specific TODO:
Test how appointment booking system work

### Other

Note: widgets it's something that you can copy paste and then adjust
